// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN
buildscript {
	repositories {
		jcenter()
	}

	dependencies {
		classpath 'com.android.tools.build:gradle:2.3.3'
	}
}

allprojects {
   repositories {
      flatDir {
        dirs 'libs'
      }
   }
}

apply plugin: 'com.android.application'

dependencies {
	compile fileTree(include: ['*.jar'], dir: 'libs')
	compile(name: 'NirvanaAndroid', ext: 'aar')
	compile 'com.android.support:appcompat-v7:26.0.0-alpha1'
    compile 'com.android.support:support-v4:26.0.0-alpha1'
}

android {
	signingConfigs {
		ylkey {
			keyAlias 'ylkey'
			keyPassword 'KR5zks3kBz'
			storeFile file('$rootDir/../../youyan.keystore')
			storePassword 'RvjF14S7yt'
            v2SigningEnabled false
		}
	}
	compileSdkVersion 26
	buildToolsVersion '26.0.1'
	defaultConfig {
		targetSdkVersion 23
		versionCode 18122600
		versionName '1.0.0'
		signingConfig signingConfigs.ylkey

	}
	lintOptions {
		abortOnError false
	}
	buildTypes {
		debug {
			minifyEnabled false
			useProguard false
			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
			jniDebuggable true
			signingConfig signingConfigs.ylkey
		}
		release {
			minifyEnabled false
			useProguard true
			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
			signingConfig signingConfigs.ylkey
		}
	}
	productFlavors {
	}
}
